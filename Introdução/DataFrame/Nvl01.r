produtos <- data.frame(
  frutas = c("Maca", "Pêra", "Banana"),
  preco = c(20,30,40),
  quantidade = c(3,5,20)
)
print(produtos)


#“<- é usado para atribuir valores a objetos; = é usado para nomear argumentos dentro de funções (embora também funcione para atribuição, não é a forma recomendada).”
